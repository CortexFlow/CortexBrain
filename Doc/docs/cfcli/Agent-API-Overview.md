# Agent API Overview

!!! info "Moved"
    The Agent API documentation has been moved to the [Developer Guide - Agent API Overview](../developer-guide/agent-api.md). That page documents all 6 `agent.Agent` gRPC RPCs, the BPF maps the agent reads, the data flow from eBPF kernel programs to `cfcli`, and the relationship with the Identity service.